1. Enable the "Interpret ASCII control characters" and "Interpret Carriage Return (\r) as control character" checkboxes in Window -> Preferences -> Run/Debug -> Console.
2. (Optional) Increase stack size to 16 MB in Run -> Run Configurations -> Arguments -> VM Arguments by typing "-Xss16m" in the VM arguments text box.